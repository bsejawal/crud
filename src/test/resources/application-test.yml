spring:
  datasource:
    url: jdbc:h2:mem:test_schema
    driver-class-name: org.h2.Driver
    username: a
    password: a
    jpa:
      database-platform: org.hibernate.dialect.H2Dialect
      hibernate:
        ddl-auto: create-drop
      show-sql: true
  h2:
    console.enabled: true